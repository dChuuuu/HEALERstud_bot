version: '3.8'

services:

  web:
    build: .
    ports:
      - "55556:55556"
    environment:
      - DATABASE_URL=postgresql://postgres:8565@db_1/healerstud_db
    depends_on:
      - db_1
    volumes:
      - .:/app
    networks:
      - healerstud_network

  migrate:
    build: .
    command: alembic upgrade head
    depends_on:
      - db_1
    environment:
      - DATABASE_URL=postgresql://postgres:8565@db_1/healerstud_db
    volumes:
      - .:/app
    networks:
      - healerstud_network


  db_1:
    image: postgres:16
    environment:
      - POSTGRES_DB=healerstud_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=8565
    ports:
      - "55555:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - healerstud_network

volumes:
  postgres_data:

networks:
  healerstud_network:
    driver: bridge